apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-http-config
data:
  default.conf: |
    server {
        listen 80;
        server_name homework.netology;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }
    }
